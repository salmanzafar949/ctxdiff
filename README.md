# ctxdiff

**git diff for your agent's context window.** A local-first context-window debugger for LLM agents: capture every call's context as content-hashed blocks, then diff, profile tokens, and inspect prompt-cache alignment turn by turn.

Status: early development (M1 — capture + store).

## License

Apache-2.0.
